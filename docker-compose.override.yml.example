version: '3.8'

services:
  app:
    ports:
      - "3000:3000" # Change host port if 3000 is in use, e.g. "3001:3000"

  redis:
    ports:
      - "6380:6379" # Exposed on host port 6380 to avoid collision with local Redis on 6379
